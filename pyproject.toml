[project]
name = "non-domination-governance-kernel"
version = "0.1.0"
description = "A formal governance kernel for detecting domination in structural agents and frame-bearing participants."
requires-python = ">=3.9"
license = {text = "MIT"}
authors = [{name = "Jonathan Rademacher"}]
readme = "README.md"

[tool.pytest.ini_options]
pythonpath = ["."]
testpaths = ["tests"]
